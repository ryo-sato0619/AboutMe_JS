【基本設計】

1. 全体アーキテクチャ図
コード スニペット

graph TD
    subgraph Log Sources
        A[Microsoft Purview Audit Logs] -->|Microsoft Graph Security API (with Pagination)| B(Logic App)
        C[Intune Audit Logs] -->|Microsoft Graph API| B
    end

    subgraph Azure
        B --Trigger: Daily--> D(Logic App)
        D --Authenticate with Managed Identity--> E(Azure Blob Storage)
        E --Lifecycle Management Policy--> F(Deletion after 3 years)
        D --On Failure / On Success--> G(Office 365 Outlook / SendGrid)
    end

    subgraph Monitoring
        D --Logs--> H(Azure Monitor Logs)
        H --Alert Rule--> G
    end

    G[Mailing List]
2. コンポーネント詳細
2.1. 監査ログ取得元
2.1.1. Microsoft Purview 監査ログ
取得方法:

推奨: Microsoft Graph Security API (/security/auditLogs/query) を利用。

件数制限対応: API応答に含まれる nextLink プロパティを利用したページネーションを実装し、指定された期間の全てのログを網羅的に取得します。Logic Apps の Until コントロールを使用して、nextLink がなくなるまでAPI呼び出しを繰り返します。

必要なAPIパーミッション: AuditLog.Read.All (Microsoft Graph Security API の場合)

2.1.2. Intune 監査ログ
取得方法:

推奨: Microsoft Graph API (/deviceManagement/auditEvents) を利用。

件数制限対応: Purview と同様に、API応答に含まれる nextLink プロパティを利用したページネーションを実装し、全てのログを網羅的に取得します。

必要なAPIパーミッション: DeviceManagementApps.Read.All, DeviceManagementConfiguration.Read.All, DeviceManagementManagedDevices.Read.All など、取得したいログの種類に応じた適切なパーミッション。

2.2. Logic Apps
監査ログの取得、加工（CSV変換）、格納、および通知を自動化するワークフローを設計します。

2.2.1. トリガー
種類: Recurrence (繰り返し)

設定:

Frequency: Day

Interval: 1

At these hours: 20 (日本時間の午後8時)

At these minutes: 0

Time zone: (UTC+09:00) Osaka, Sapporo, Tokyo (日本標準時)

2.2.2. 認証
推奨: マネージドID (Managed Identity) を使用。

種類: システム割り当てマネージドID

設定: Logic App リソースの「ID」ブレードで「システム割り当て済み」を「オン」に設定。

必要なロール割り当て:

Azure Blob Storage: Storage Blob データ共同作成者 または Storage Blob データ所有者 (コンテナーへの書き込み権限)。

Microsoft Graph API: Azure AD アプリケーション登録を作成し、必要なAPIパーミッションを付与。Logic App のマネージドIDに、そのアプリケーション登録のサービスプリンシパルに対する適切なロール (例: アプリケーション所有者) を付与するか、Graph API 呼び出し時にマネージドIDで直接認証できるコネクタ/カスタムコネクタを使用。

マネージドIDが不可の場合の考慮:

個人に紐づく認証情報を使用する際は、専用のサービスアカウントやグループアカウントの利用を検討し、特定の個人への依存を減らします。

認証情報の定期的な棚卸しと更新プロセスを確立し、異動や退職に備えます。

2.2.3. 監査ログ取得アクション
アクション: HTTP アクション (Microsoft Graph Security API 呼び出し用)

設定例 (Purview の場合):

Method: POST (Graph Security APIの auditLogs/query はPOSTを使用)

URI: https://graph.microsoft.com/v1.0/security/auditLogs/query

Headers: Content-Type: application/json

Body: 取得したいログの条件を指定します。例えば、特定の日付範囲やオペレーションタイプなど。

JSON

{
  "query": "Service=Microsoft365ComplianceAudit&Operations=FileAccessed",
  "startTime": "@{formatDateTime(addDays(utcNow(), -1), 'yyyy-MM-ddT00:00:00Z')}",
  "endTime": "@{formatDateTime(utcNow(), 'yyyy-MM-ddT00:00:00Z')}",
  "limit": 50000
}
startTime と endTime は、前日分のログを取得する Logic Apps の実行日時に応じて動的に生成します。

limit は一度に取得する最大件数を指定します。

Authentication: Managed Identity

Audience: https://graph.microsoft.com

ページネーション処理:

HTTP アクションの後に Until (〜まで繰り返す) コントロールを配置します。

Until ループの条件は、API応答の body('HTTP_アクション名')['@odata.nextLink'] が空ではない間、と設定します。

ループ内で HTTP アクションを繰り返し呼び出し、nextLink のURLを使用します。

取得した各ページのログデータを変数に追記し、全てのログを結合します。

エラー処理:

HTTP アクションの応答ステータスコードを確認し、エラー (例: 4xx, 5xx) の場合は後続の処理をスキップし、エラー通知フローに分岐させます。

2.2.4. データ加工（CSV変換）
アクション: Data Operations - Parse JSON (取得したログデータをJSONオブジェクトに変換)

アクション: Data Operations - Create CSV table

Create CSV table アクションは、JSON配列を入力として受け取り、CSV形式の文字列を生成します。

ネストされたJSON構造や複雑な変換が必要な場合は、Azure Function を利用することを検討します。

Azure Function (Python/C#など) でJSONデータを詳細にパースし、CSV形式に整形するロジックを実装します。

Logic Apps から Azure Functions コネクタを介してこのFunctionを呼び出し、JSONデータを渡し、CSVデータを受け取ります。

2.2.5. Azure Blob Storage への格納アクション
アクション: Azure Blob Storage - Create blob (V2)

設定:

Storage account name: (対象のストレージアカウント名)

Folder path: (コンテナー名)/[サービス名]/[年]/[月]/[日]

例: purview-audit-logs/@{formatDateTime(utcNow(), 'yyyy')}/@{formatDateTime(utcNow(), 'MM')}/@{formatDateTime(utcNow(), 'dd')}

Blob name: [サービス名]_[YYYYMMDD]_[HHMMSS].[拡張子]

例: purview_@{formatDateTime(utcNow(), 'yyyyMMdd_HHmmss')}.csv

Blob content: (CSV変換後のデータ)

Blob type: Block

Access tier: Hot

2.2.6. 通知（完了/エラー）
「スコープ」コントロールの配置:

監査ログ取得からBlob Storageへの格納までの一連のアクションを「スコープ」コントロールで囲みます。

スコープ名: Log_Collection_and_Storage_Scope など、分かりやすい名前にします。

成功時の完了通知:

スコープの後に「条件 (Condition)」コントロールを追加します。

条件式: @{equals(outputs('Log_Collection_and_Storage_Scope').status, 'Succeeded')}

「はい」の場合の処理:

アクション: Office 365 Outlook - Send an email (V2) または SendGrid - Send Email

To: (所定のメーリングリストのアドレス)

Subject: [完了] Logic App 監査ログ取得完了 - @{workflow().name}

Body: 処理が正常に完了した旨、Logic App の実行ID、完了日時、および格納されたログの概要（例: 格納されたファイル数、合計サイズなど、可能な範囲で）を含めます。

失敗時のエラー通知:

スコープの後に別の「条件 (Condition)」コントロールを追加するか、または上記の「条件」コントロールの「いいえ」のパスでさらに条件分岐させます。

条件式: @{equals(outputs('Log_Collection_and_Storage_Scope').status, 'Failed')}

「はい」の場合の処理:

アクション: Office 365 Outlook - Send an email (V2) または SendGrid - Send Email

To: (所定のメーリングリストのアドレス)

Subject: [緊急] Logic App 監査ログ取得エラー発生 - @{workflow().name}

Body: 発生したエラーの種類、Logic App の実行ID、エラー発生日時、関連するログ取得元、およびエラーの詳細（@{json(result('Log_Collection_and_Storage_Scope'))} など）を含めます。

2.3. Azure Blob Storage
監査ログの格納とライフサイクル管理を行います。

2.3.1. ストレージアカウント
種類: General-purpose v2 (GPv2)

冗長性: Locally-redundant storage (LRS) または Zone-redundant storage (ZRS) (可用性要件に応じて選択)

アクセス層: Hot (初期設定)

2.3.2. コンテナー
命名規則: サービスごとに分離し、小文字で統一。

purview-audit-logs

intune-audit-logs

2.3.3. ログの命名ルール (再掲)
形式: [サービス名]/[年]/[月]/[日]/[サービス名]_[YYYYMMDD]_[HHMMSS].[拡張子]

例:

purview-audit-logs/2025/07/28/purview_20250728_200000.csv

intune-audit-logs/2025/07/28/intune_20250728_200000.csv

3. セキュリティ考慮事項
最小特権の原則: Logic Apps のマネージドIDに付与するロールは、必要な最小限の権限に制限します。

ネットワークセキュリティ:

ストレージアカウントへのアクセスを、Logic Apps がデプロイされているVNetからのアクセスに制限する (プライベートエンドポイントまたはサービスエンドポイント)。

Logic Apps のアウトバウンドIPアドレスを固定し、必要に応じてファイアウォールルールを設定。

シークレット管理: APIキーや接続文字列などのシークレットは、Logic Apps のコードに直接ハードコードせず、Azure Key Vault に格納し、Logic Apps から参照するようにします。

4. 監視とアラート
Azure Monitor Logs (Log Analytics Workspace):

Logic Apps の診断設定を有効にし、すべてのログをLog Analytics Workspaceに送信します。

Kusto Query Language (KQL) を使用して、以下のクエリで監視します。

Logic App 実行成功/失敗:

コード スニペット

AzureDiagnostics
| where ResourceProvider == "MICROSOFT.LOGIC"
| where Category == "WorkflowRuntime"
| where resource_workflowName_s == "YourLogicAppName" // Logic App 名を指定
| summarize RunCount = count() by status_s
特定のアクションの失敗:

コード スニペット

AzureDiagnostics
| where ResourceProvider == "MICROSOFT.LOGIC"
| where Category == "WorkflowRuntime"
| where resource_workflowName_s == "YourLogicAppName"
| where status_s == "Failed"
| mv-expand properties_action_s
| where properties_action_s.status == "Failed"
| project TimeGenerated, resource_workflowName_s, properties_action_s.actionName, properties_action_s.error
Azure Monitor アラート:

アラートルール: Log Analytics Workspace 上で、上記のKQLクエリを基にアラートルールを作成します。

条件: 例: 過去5分間に Logic App の実行失敗が1回以上発生した場合。

アクショングループ: メーリングリストへのメール通知を設定したアクショングループを割り当てます。

5. コスト最適化
ストレージコスト:

3年間の保存期間中、アクセス頻度が低下する過去のログに対しては、ライフサイクル管理ポリシーで Hot → Cool → Archive 層への段階的な移行を検討します。これにより、ストレージコストを大幅に削減できます。

例:

最終変更後 30日経過: Hot から Cool へ移行

最終変更後 90日経過: Cool から Archive へ移行

最終変更後 1095日経過: 削除

Logic Apps コスト:

実行回数とアクション数に基づいて課金されます。不要な実行や冗長なアクションがないか定期的に見直します。

6. デプロイと運用
デプロイ:

Infrastructure as Code (IaC): Azure Resource Manager (ARM) テンプレート、Bicep、または Terraform を使用して、Logic Apps、ストレージアカウント、ライフサイクル管理ポリシー、監視設定などをコードとしてデプロイすることを推奨します。これにより、再現性と一貫性が確保されます。

運用:

定期的な監視: Azure Monitor を活用し、Logic Apps の実行状況、エラー、ストレージの使用状況などを定期的に監視します。

ログの確認: 必要に応じて、Azure Portal の Blob Storage または Azure Storage Explorer を使用して、格納されたログの確認を行います。

ライフサイクルポリシーの確認: ポリシーが意図通りに動作しているか、定期的に確認します。

API変更への対応: Microsoft Graph API や Purview API の変更があった場合、Logic Apps のワークフローを適宜更新する必要があります。


【詳細設計】
1. 全体アーキテクチャ図
コード スニペット

graph TD
    subgraph Log Sources
        A[Microsoft Purview Audit Logs] -->|Microsoft Graph Security API (with Pagination)| B(Logic App)
        C[Intune Audit Logs] -->|Microsoft Graph API (with Pagination)| B
    end

    subgraph Azure
        B --Trigger: Daily--> D(Logic App)
        D --Authenticate with Managed Identity--> E(Azure Blob Storage)
        E --Lifecycle Management Policy--> F(Deletion after 3 years)
        D --On Failure / On Success--> G(Office 365 Outlook / SendGrid)
    end

    subgraph Monitoring
        D --Logs--> H(Azure Monitor Logs)
        H --Alert Rule--> G
    end

    G[Mailing List]
2. コンポーネント詳細
2.1. Azure リソースグループ
リソースグループ名: rg-audit-log-prod-central

用途: 本システムを構成する全てのAzureリソース（Logic Apps、ストレージアカウント、Key Vaultなど）を配置し、一元管理、アクセス制御、およびコスト追跡を行います。

2.2. Logic Apps
監査ログの取得、加工、格納、および通知を自動化するワークフローを設計します。

2.2.1. トリガー
種類: Recurrence (繰り返し)

設定:

Frequency: Day

Interval: 1

At these hours: 20 (日本時間午後8時)

At these minutes: 0

Time zone: (UTC+09:00) Osaka, Sapporo, Tokyo

2.2.2. 認証
方式: マネージドID (Managed Identity)

設定: Logic App リソースの「ID」ブレードで、システム割り当て済みマネージドIDを有効にします。

必要なRBACロール割り当て:

対象: Logic App のマネージドID

スコープ: rg-audit-log-prod-central リソースグループ

ロール:

Storage Blob データ共同作成者: Azure Blob Storage への読み書き権限。

アプリケーション所有者（または同等の権限）：Microsoft Graph APIへのアクセスに必要なAzure ADアプリケーション登録に付与。

2.2.3. 監査ログ取得とページネーションのワークフロー
変数初期化:

varLogData (Array型): 取得したログデータを一時的に格納する配列。

varNextLink (String型): ページネーションのための nextLink URLを格納。

ログ取得 (Purview) - Until ループ:

コントロール: Until (〜まで繰り返す)

条件: varNextLink が空ではない (@not(equals(variables('varNextLink'), '')))

アクション（ループ内）:

HTTPアクション: POST メソッドで Microsoft Graph Security API (/security/auditLogs/query) を呼び出します。

リクエストボディ:

JSON

{
  "query": "Service=Microsoft365ComplianceAudit&Operations=FileAccessed",
  "startTime": "@{formatDateTime(addDays(utcNow(), -1), 'yyyy-MM-ddT00:00:00Z')}",
  "endTime": "@{formatDateTime(utcNow(), 'yyyy-MM-ddT00:00:00Z')}",
  "limit": 50000
}
初回は固定の開始・終了日時、2回目以降はnextLinkを使用します。

応答処理:

Parse JSON アクションで応答ボディをパース。

nextLink が存在する場合、varNextLink 変数を更新します。

Compose アクションでログデータを抽出・整形し、varLogData 配列変数に追加します。

Intune ログも同様にページネーションで取得し、varLogData に結合します。

2.2.4. データ加工 (CSV変換)
CSVヘッダー定義:

ログデータから抽出するフィールドを決定し、CSVのヘッダーを定義します。

例: Timestamp,UserId,Operation,ResultStatus

JSONからCSVへの変換:

Data Operations - Create CSV tableアクション:

From: varLogData

Headers: Custom を選択し、定義したヘッダーを配列で入力します。

詳細: シンプルなJSON構造の場合は有効です。

Azure Function (推奨):

言語: Python/C#

ロジック: varLogData をJSON配列として受け取り、CSVライブラリを使用してCSV文字列に変換します。ネストされたJSONをフラット化し、ヘッダーに合うようにフィールドをマッピングするロジックを実装します。

2.2.5. Azure Blob Storage への格納
アクション: Azure Blob Storage - Create blob (V2)

設定:

Storage account name: (対象のストレージアカウント名)

Folder path: purview-audit-logs/@{formatDateTime(utcNow(), 'yyyy')}/@{formatDateTime(utcNow(), 'MM')}/@{formatDateTime(utcNow(), 'dd')}

Blob name: purview_@{formatDateTime(utcNow(), 'yyyyMMdd_HHmmss')}.csv

Blob content: CSV変換後のデータ

Blob type: Block

2.3. Azure Blob Storage
ストレージアカウント: General-purpose v2 (GPv2)

冗長性: LRS

アクセス層: Hot

コンテナー:

purview-audit-logs

intune-audit-logs

ライフサイクル管理ポリシー:

ルール:

ルール名: DeleteBlobsAfter1095Days

フィルター: purview-audit-logs コンテナー内の ブロック BLOB

アクション: 最終変更から 1095 日後に BLOB を削除する

3. セキュリティ考慮事項
Key Vault:

rg-audit-log-prod-central リソースグループ内にAzure Key Vaultをデプロイします。

APIキーや接続文字列などのシークレットはKey Vaultに格納し、Logic Apps から参照するように構成します。

Logic Apps のマネージドIDに、Key Vaultの「シークレットの取得」権限を付与します。

ネットワーク:

ストレージアカウントで「選択された仮想ネットワークとIPアドレスから有効」なアクセスを許可する設定を行い、Logic AppsのアウトバウンドIPアドレスを許可リストに追加します。

4. 監視とアラート
Logic Apps 診断設定:

Logic App の診断設定を有効化し、WorkflowRuntime カテゴリのログを Log Analytics Workspace に送信するように構成します。

Azure Monitor アラート:

アラートルール: Logic App の実行ログをクエリし、status_s == "Failed" の場合にアラートを発生させます。

アクショングループ: 担当者やメーリングリスト宛てのメール通知をアクショングループとして定義し、アラートルールに紐づけます。

5. 運用と保守
デプロイメント:

ARMテンプレートまたはBicepファイルを作成し、リソースグループ、Logic Apps、ストレージアカウント、およびRBACロール割り当てをコードとしてデプロイします。

これにより、開発環境 (rg-audit-log-dev-central) と本番環境を簡単に同期・管理できます。

定期的なレビュー:

ログ量やAPI仕様の変更に備え、年に一度など定期的に本設計を見直します。




別件（メッセージセンターの情報を取得）
【基本設計】
1.1. システム概要
Microsoft 365 メッセージセンターの情報を、Power AutomateのフローでLogic Appsを中継役として活用し、SharePoint Onlineのカスタムリストに自動連携するシステムです。Logic Appsは、Microsoft Graph APIへのアクセスを一手に担い、認証やデータ取得のロジックを管理します。これにより、Power AutomateはLogic Appsを呼び出すことに専念し、DLPポリシーの影響を回避しつつ、安定した運用を実現します。

1.2. システム構成
Microsoft Graph API: メッセージセンターの詳細情報を取得するためのAPIです。

Logic Apps (標準):

メッセージセンターの情報を取得する中継役です。

マネージドIDを使ってMicrosoft Graph APIに認証します。これにより、クライアントシークレットの管理が不要となり、セキュリティが向上します。

Power AutomateからのHTTPリクエストを受け取り、Graph APIからの応答をJSON形式でPower Automateに返します。

従量課金制のため、実行した分だけ費用が発生します。

Power Automate (クラウドフロー):

スケジュールトリガーで定期的に起動します。

標準のHTTPコネクタ（E3ライセンスで利用可能）を使ってLogic Appsを呼び出します。

Logic Appsから返されたJSONデータを解析し、SharePoint Onlineリストに作成・更新します。

SharePoint Online: 連携されたメッセージ情報を格納するカスタムリストです。

コード スニペット

graph TD
    A[Microsoft 365 Message Center] --> B[Microsoft Graph API];
    B -- GET messages --> C[Logic Apps];
    C -- Managed Identity --> D[Microsoft Entra ID];
    E[Power Automate<br>(Cloud Flow)] -- HTTP Request (Standard Connector) --> C;
    C -- JSON Response --> E;
    E -- Create/Update List Item --> F[SharePoint Online List];

    subgraph Azure Cloud
        C
        D
    end
    subgraph M365 Services
        A
        B
        F
        E
    end
1.3. 運用上の考慮事項
コスト: Logic Appsは従量課金制です。メッセージ連携の頻度やデータ量によっては、低額な費用が発生します。無料枠を超える可能性があるため、コスト監視を推奨します。

セキュリティ: Logic AppsはマネージドIDを使用するため、安全にGraph APIにアクセスできます。ただし、Power AutomateからLogic Appsを呼び出す際の認証（例: 共有アクセス署名（SAS）トークンやAPIキー）の管理が必要です。

メンテナンス: Logic Appsのコードや設定はAzure Portalで管理します。Power Automateとは異なる環境のため、管理者権限とメンテナンスの計画が必要です。

【詳細設計】
2.1. SharePoint Online リストの作成
この部分は、Power Automate単独の設計と同じです。

サイト: https://[テナント名].sharepoint.com/sites/YourSite

リスト名: M365メッセージセンター

推奨列:

メッセージID (一行テキスト): 重複禁止設定推奨

タイトル (一行テキスト)

カテゴリ (一行テキスト)

サービス (複数行テキスト)

影響種別 (一行テキスト)

ステータス (一行テキスト)

公開日 (日付と時刻)

最終更新日 (日付と時刻)

終了予定日 (日付と時刻)

詳細 (複数行テキスト)

詳細リンク (ハイパーリンクまたは画像)

2.2. Logic Apps（標準）設定・実装詳細
Azure Portalでロジックアプリを作成:

プラン: 標準

ホスト: 従量課金（コスト効率のため）

マネージドIDの構成: 作成したロジックアプリにシステム割り当てマネージドIDを有効にします。

マネージドIDへのGraph API権限付与:

Microsoft Entra IDの「エンタープライズアプリケーション」で、作成したロジックアプリのサービスプリンシパルを検索します。

そのサービスプリンシパルの「アクセス許可」から、「Microsoft Graph」の「アプリケーションのアクセス許可」で「ServiceMessage.Read.All」を追加し、管理者の同意を与えます。

Logic Appsフローの実装:

トリガー: HTTP リクエストが受信されたとき

要求本文のJSONスキーマ: Power Automateから送られてくるデータのスキーマ（例: { "type": "object", "properties": { "lastRunDate": { "type": "string" } } }）を設定します。

Microsoft Graphアクション:

アクション: HTTP アクション を選択します。

認証: 「マネージドID」 を選択し、Graph APIのリソースURL (https://graph.microsoft.com) を指定します。

メソッド: GET

URI: https://graph.microsoft.com/beta/admin/serviceAnnouncement/messages?$filter=lastModifiedDateTime ge '[トリガーから受け取ったlastRunDate]'

ヘッダー: Accept-Language: ja-JP

応答アクション:

アクション: 応答

状態コード: 200

ヘッダー: Content-Type: application/json

本文: Graph APIから取得したHTTP アクションの本文をそのまま返します。

2.3. Power Automate フロー詳細設計
この部分は、これまでのPower Automate単独の設計とほとんど同じですが、HTTPコネクタで呼び出す先がLogic Appsに変更されます。

フローの種類: スケジュール済みクラウド フロー

トリガー: 繰り返し（例: 1日ごと）

変数の初期化: lastRunDate, sharePointSiteUrl, sharePointListName を設定します。

Logic Appsへの呼び出し:

アクション: 標準の「HTTP」コネクタの「HTTP 要求を送信します」アクション

メソッド: POST

URI: Logic AppsのHTTP リクエストトリガーが提供するURLを指定します。

ヘッダー: Content-Type: application/json

本文: {"lastRunDate": "@{variables('lastRunDate')}"} のように、次回以降の実行で使うlastRunDateを渡します。

JSON の解析: Logic Appsから返されたJSON応答を解析します。

Apply to each: 解析したデータから、メッセージごとにループ処理します。

SharePoint連携ロジック: SharePointコネクタを使って、アイテムの存在確認、新規作成、または更新を行います。

最終実行日時の更新: lastRunDate変数を utcNow() で更新します。


{
  "query": "Service=Microsoft365ComplianceAudit",
  "startTime": "@{formatDateTime(addDays(utcNow(), -1), 'yyyy-MM-ddT00:00:00Z')}",
  "endTime": "@{formatDateTime(utcNow(), 'yyyy-MM-ddT00:00:00Z')}",
  "limit": 50000 
}

# 証明書を作成し、Current Userの個人ストアに保存します
$cert = New-SelfSignedCertificate -Subject "CN=LogicApp-GraphAPI-Test-Cert" -KeyExportPolicy Exportable -CertStoreLocation "Cert:\CurrentUser\My"

# 証明書のパスワードを設定します（安全なパスワードに変更してください）
$pfxPassword = ConvertTo-SecureString -String "YOUR_SAFE_PASSWORD_HERE" -Force -AsPlainText

# 秘密鍵を含む証明書（.pfxファイル）をエクスポートします
# このファイルはAzure Key Vaultにアップロードします
Export-PfxCertificate -Cert $cert -FilePath C:\temp\logic-app-cert.pfx -Password $pfxPassword

# 公開鍵のみの証明書（.cerファイル）をエクスポートします
# このファイルはMicrosoft Entra IDにアップロードします
Export-Certificate -Cert $cert -FilePath C:\temp\logic-app-cert.cer


3. 監査ログ取得のためのHTTPアクションの追加
Graph APIを呼び出して監査ログを取得するアクションを追加します。

HTTP 要求の受信時トリガーの下にある「+ 新しいステップ」をクリックします。

検索ボックスに「HTTP」と入力し、組み込みの「HTTP」アクションを選択します。

以下の設定を行います。

Method: POST

URI: https://graph.microsoft.com/v1.0/security/auditLogs/query

ヘッダー:

キー: Content-Type

値: application/json

本文:

JSON

{
  "startTime": "@{formatDateTime(addDays(utcNow(), -1), 'yyyy-MM-ddT00:00:00Z')}",
  "endTime": "@{formatDateTime(utcNow(), 'yyyy-MM-ddT00:00:00Z')}"
}
💡 この例では過去24時間のログを取得します。取得したい期間に合わせてstartTimeとendTimeを調整してください。

認証: 以下の設定を展開して行います。

認証の種類: マネージド ID

オーディエンス: https://graph.microsoft.com

{
  "query": "Service=Microsoft365ComplianceAudit",
  "startTime": "@{variables('varStartTime')}",
  "endTime": "@{variables('varEndTime')}"
}